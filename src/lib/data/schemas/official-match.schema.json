{
    "$schema": "http://json-schema.org/draft-07/schema",

    "type": "object",
    "required": ["id", "date", "lineups", "maps"],
    "additionalProperties": false,
    "properties": {
        "id": { "type": "number" },
        "date": { "type": "string", "format": "date" },
        "lineups": { "$ref": "#/$defs/lineups" },
        "links": { "type": "array", "items": { "$ref": "extern-link.schema.json" } },
        "maps": { "type": "array", "items": { "$ref": "official-map.schema.json" } },
        "note": { "type": "string" }
    },

    "$defs": {
        "lineups": {
            "type": "object",
            "required": ["team1", "team2"],
            "additionalProperties": false,
            "properties": {
                "team1": { "$ref": "official-lineup.schema.json" },
                "team2": { "$ref": "official-lineup.schema.json" }
            }
        }
    }
}
